<h1>Cowabunga Umbrellas</h1>
<% @products.each do |product| %>
  <h3><%= product.title %></h3>
  <h4><%= number_to_currency(product.price) %></h4>
  <img src="<%= product.image %>" width="100" >

<% end %>